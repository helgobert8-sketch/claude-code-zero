<!--
NEXT_SESSION.md — pending Prompt für die nächste fokussierte Session.
Konvention: EINE Datei, immer überschrieben (nie angehängt, keine Timestamps) → kein Müll.
Trigger: Auto-Pickup via CLAUDE.md bei Session-Start, oder "Führ NEXT_SESSION.md aus".
Selbst-Reset: siehe Footer.
-->

# Pending Prompt — Linear-Build-Achse: Pilot steht, nächster Task = höchste Prio in Linear

**Linear-Build-Achse (`project_linear_build_axis`) — Job 1 + Job 2 DONE.** Der Dashboard-Forward-State lebt jetzt im Linear-Projekt **„Dashboard"** (Team **Pan-Opticon**, Web-Connector `mcp__claude_ai_Linear__*`): 11 Issues PAN-5..PAN-15, Label-Gruppen `domain {ai|macro|infra}` + `type {build|research|monitoring|chore}`, alle Backlog.

**Modell:** Wissen → CLAUDE.md · Record → `dashboard_build_log.md` · **State/offene Tasks → Linear**. Der ~50er-Watch-Korpus bleibt Single-Source-of-Truth in `retrofit.json`; nur 4 Sammel-Watches sind nach Linear gespiegelt (Hybrid). GitHub-Linear-Integration bewusst SKIPPED (direct-to-master, kein PR-Flow) — nachholbar, wenn Codex-Reviewer/PR-Flow real wird.

**Nächster Task = höchste offene Prio im Linear-Projekt „Dashboard":**
- **High (Watches, passiv):** PAN-12 Fable/Mythos-Export-Control-Resolution · PAN-13 Öffentliche S-1 (Anthropic+OpenAI, post-SEC).
- **Medium (aktiv, je eigene Session):** PAN-5 Pal-Profil-Update-Pass-2 (Macro, nicht zwischen AI-Blöcke) · PAN-6 SemiAnalysis-Volltext-Patel-Session (AI).

**Wichtig — zwei NEXT_SESSION.md:** Diese hier (ClaudeCodeZero) steuert die Build-/Tooling-Achse. Die **laufende AI-Dashboard-Analyse-Kadenz** hat ihre eigene Datei `C:\Users\manyw\Dashboard\NEXT_SESSION.md` (Stand: passiv/Beobachtung — Fable-Resolution, S-1, Korea-Pivot, needs_fulltext). Für einen Dashboard-Inhalts-Pass dort starten, nicht hier.

**Optional Job 3 (wenn gewünscht):** Linear-Workflow im Alltag einüben — Issue beim Start auf „In Progress", beim Abschluss „Done"; bei neuen offenen Strängen Issue statt CLAUDE.md-Notiz. Bei Bedarf GitHub-Linear-Integration nachziehen.

---

> **Selbst-Reset (Instruktion an die ausführende Session, nicht Teil des Prompts):** Wenn der nächste Task durch ist, überschreibe `NEXT_SESSION.md` mit dem nächsten Folge-Prompt — oder, falls nichts ansteht, ersetze den Inhalt durch `# Pending Prompt — (leer)`. Niemals anhängen; immer überschreiben (eine Datei, kein Müll). Git hält die Historie.
